{
    "connections": {
        "default": {
            "engine": "tortoise.backends.asyncpg",
            "credentials": {
                "host": "localhost",
                "port": 5432,
                "user": "postgres",
                "password": "postgres",
                "database": "history"
            }
        }
    },
    "apps": {
        "models": {
            "models": [
                "history.models"
            ]
        }
    },
    "use_tz": true,
    "timezone": "UTC"
}